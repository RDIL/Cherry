plugins {
    id "java"
    id "fabric-loom" version "0.5-SNAPSHOT"
}

subprojects {
    jar {
        from "../LICENSE"
    }

    tasks.withType(JavaCompile) {
        options.encoding = "UTF-8"
    }
}
